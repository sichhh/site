<h1>Результаты поиска</h1>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= user.first_name %> <%= user.last_name %> (<%= user.email %>)
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Пользователи не найдены.</p>
<% end %>   
